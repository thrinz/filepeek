[pytest]
testpaths = tests
markers =
    e2e: browser end-to-end tests (need playwright + chromium installed)
; unit/API tests by default; run the browser suite with:  pytest -m e2e
addopts = -m "not e2e"
