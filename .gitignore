.venv/
__pycache__/
.pytest_cache/
.claude/settings.local.json

# Local marketing drafts — not part of the repo
posts/
bookmarks.json
permlinks.json
nohup.out

# filepeek runtime state (created when STATE_DIR is the app dir) — may hold the S3 secret
backup_config.json
backup.log

# Secrets — never commit credentials or env files
.env
*.env
filepeek.env
filepeek-credentials.txt
*credentials*
*.pem
*.key
